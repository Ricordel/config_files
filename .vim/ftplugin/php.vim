"=========================== Des options pour le PHP =========================

" Sans que je sache pourquoi, le cursorline fait ramer les fichiers PHP
" (langage de merde jusqu'au bout qui arrive même à faire ramer Vim)
set nocursorline

let php_sql_query=1				" Coloration des requêtes SQL dans le code php
let php_htmlInStrings=1			" Coloration du HTML dans les instructions 'echo'
